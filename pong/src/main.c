#include <stdio.h>
//#include <global.h>
#include <string.h>

void main(void) {
    for (;;)
    {
        int i;
        for (i = 0; i != -1; i++);
        printf("@@@@@@@@@@ pong! @@@@@@@@@@\n");
        //DUMP(*((dword_t*) 0x80000000));
    }
}
